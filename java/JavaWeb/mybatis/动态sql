动态sql

通过表达式进行判断，对sql进行灵活拼接，组合

select * from user
<where> 当后面的条件不满足时去掉where条件按
	<if test="">
		<if test="">

		</if>
	</if>
</where>

--sql片段
经验：基于单表来定义sql片段（重用性高）
在sql片段中不要包含where

<sql id="">	
</sql>

--引用sql片段
<include refid=""/>

--foreach标签
<foreach collection="" item="" open="" separator="">
</foreach>



